include includes/layout.pug
+var

+head

body
    +header('top')

    +main
        div.jumbotron
            h1.display-3 Hellow, World!

    +footer([{
        current: true,
        filename: 'index.html',
        title: 'Top'
    }])
